# Web / API - Pinned for Railway build speed
fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic==2.11.3
python-multipart==0.0.20
aiofiles==24.1.0
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4

# AI - API-based Online Providers (No heavy local models)
google-genai==0.3.0
openai==1.35.0
# REMOVED: langchain (heavy, not used directly)
langchain-text-splitters==0.0.1
anthropic==0.28.0

# Vector DB - Using lightweight client
chromadb==0.5.0
# NOTE: sentence-transformers is heavy (500MB+ PyTorch)
# It's optional - reranker falls back gracefully if not installed
# Install manually only if needed: pip install sentence-transformers

# Document parsing - Pinned versions
PyPDF2==3.0.0
python-docx==0.8.11
beautifulsoup4==4.12.0

# Utilities - Pinned for speed
numpy==1.26.0
requests==2.31.0
aiohttp==3.13.4

# Security & Rate Limiting
slowapi==0.1.9
