# Web / API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiofiles>=23.2.0

# ML / NLP (Lightweight CPU)
# torch - REMOVED for Render memory limit
sentence-transformers>=2.2.0
chromadb>=0.4.18

# Database / Storage (Use SQLite instead of Redis/Postgres for free tier)
# redis - REMOVED
# psycopg2-binary - REMOVED

# LLM APIs (Gemini free tier, no API key needed)
google-generativeai>=0.3.0

# Document parsing
PyPDF2>=3.0.0
python-docx>=0.8.11
beautifulsoup4>=4.12.0
markdown>=3.5.0

# Utilities
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.2.0
aiohttp>=3.9.0
